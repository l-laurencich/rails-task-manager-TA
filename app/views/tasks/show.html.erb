<h1>Details about this Task</h1>

<h2><%= @task.title %></h2>
<p><%= @task.details %></p>

<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>